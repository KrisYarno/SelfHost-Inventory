{
  "name": "inventory-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only MCP sidecar over the shared lib/assistant tool layer (Lane 4). Skeleton created in T1; implemented in T5.",
  "scripts": {
    "build": "tsup",
    "start": "node dist/server.js",
    "smoke": "node dist/smoke.js",
    "test": "jest -c jest.config.mjs"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^4.0.12",
    "@ai-sdk/google": "^4.0.12",
    "@ai-sdk/openai": "^4.0.11",
    "@modelcontextprotocol/sdk": "1.29.0",
    "@prisma/client": "^6.10.1",
    "ai": "7.0.22",
    "ai-sdk-ollama": "4.0.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsup": "^8",
    "typescript": "^5"
  }
}
